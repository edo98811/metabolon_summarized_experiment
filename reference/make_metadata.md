# Clean and format the metadata from the CDT file

Clean and format the metadata from the CDT file

## Usage

``` r
make_metadata(metadata)
```

## Arguments

- metadata:

  A data frame containing the metadata from the CDT file.

## Value

A cleaned and formatted metadata data frame.
